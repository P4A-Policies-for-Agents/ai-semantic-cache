# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Salesforce, Inc.
version: "3.3"

services:
  local-flex:
    image: mulesoft/flex-gateway:1.11.0
    ports:
      - "8185:8185"
    volumes:
      - ./config:/usr/local/share/mulesoft/flex-gateway/conf.d/
    depends_on:
      - mock-service
      - qdrant

  # MockServer stands in for the OpenAI-compatible Embeddings endpoint and
  # the upstream LLM (chat/completions).
  mock-service:
    image: mockserver/mockserver:latest
    ports:
      - "8080:1080"
    volumes:
      - ./config/mockserver:/config
    environment:
      - MOCKSERVER_INITIALIZATION_JSON_PATH=/config/init.json
      - MOCKSERVER_SERVER_PORT=1080

  # Qdrant OSS — same REST surface as Qdrant Cloud, in-memory by default.
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
